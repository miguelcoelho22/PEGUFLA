services:
  db:
    image: postgres:15
    container_name: pegufla-db
    environment:
      POSTGRES_DB: pegufla_db
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: password123
    ports:
      - "5433:5432"